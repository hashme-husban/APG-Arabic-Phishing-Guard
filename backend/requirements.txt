fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
python-dotenv==1.0.1
email-validator==2.2.0
reportlab==4.2.5
psycopg2-binary==2.9.10
gunicorn==23.0.0
numpy==2.2.1
joblib==1.4.2
scikit-learn==1.6.0
torch==2.5.1
transformers==4.47.1
safetensors==0.4.5
PyYAML==6.0.2
# Dynamic URL sandbox runtime. Disabled by default unless explicitly enabled by env.
playwright==1.49.1
# OCR — backend Arabic text extraction from images
pytesseract==0.3.13
Pillow==11.0.0
httpx==0.27.2
# PaddleOCR NOTE: paddleocr==2.8.1 requires numpy<2.0, which conflicts with numpy==2.2.1
# needed by scikit-learn / torch / transformers. PaddleOCR cannot be installed in this
# environment. OCR_ENGINE=tesseract is used; set OCR_ENGINE=paddle only in a dedicated
# PaddleOCR service with numpy<2.0 and without the ML model stack.
