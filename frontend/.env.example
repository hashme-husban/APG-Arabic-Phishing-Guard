# APG Frontend — environment example
# For local development copy to frontend/.env
# For Docker Compose deployment VITE_API_BASE_URL=/api is injected at build time.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
VITE_APP_NAME=APG Admin Console
VITE_ENABLE_DEMO_MODE=false
